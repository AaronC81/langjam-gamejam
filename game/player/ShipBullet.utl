entity ShipBullet {
    use Positional;

    constructor {
        @x = 1;
        @y = 1;
    }

    tick {
        @x = @x + 1;

        if (@x > Display.width()) {
            destroy this;
        }
    }

    draw {
        return sprite { # };
    }
}
